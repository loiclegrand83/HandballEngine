---
scope: PRD haweb 2026-09-06: Board ergonomie, Bibliothèque, Séances, Calendrier
purpose: Fixer les invariants pour que les 4 features restent cohérentes (persistance, routes API, structure page/JS partagé)
altitude: feature/initiative (PRD complet, produit entier)
updated: 2026-09-06T18:36
---

- (decision) Bibliothèque client logic (fetch/filter/search/render) extracted into shared vanilla JS module, loaded via plain <script> tag by any page needing it (board, seance, planning). Server API stays single source of truth for persistence. Prevents divergent filter/search re-implementations across pages
- (decision) Séance export: pas de vraie lib PDF nécessaire, print CSS existant (window.print) suffit — le vrai défi est la mise en page du récap multi-ateliers (3-5 schémas/page), pas le format de fichier
- (decision) Recap layout: CSS grid fixe (ordre des Blocs/Ateliers), auto-remplissage, saut de page CSS auto (@media print break-inside/break-after) quand pleine. Pas de réorganisation manuelle drag/drop en v1
- (decision) Rendu schéma exercice: moteur canvas de explanation.html extrait en fonction réutilisable, appelée depuis Séance/Calendrier à partir des données items/paths brutes. Pas de snapshot PNG stocké — évite désync schéma/exercice après édition
- (decision) Calendrier data: événements légers dans data/planning/ via route /api/planning existante. Un fichier JSON par événement: {date, type: 'seance'|'match', seanceId (ref si type seance), adversaire (si match)}. Séance jamais dupliquée, référencée par id depuis data/seances/
- (constraint) Inherited from PRD prd-haweb-2026-09-06: FR-2 removes Temps mort module entirely (timeout.html/js/css deleted, no nav entry); FR-1 requires double-click/double-tap to end trajectory (board editing UX, no new architectural surface)
- (constraint) Ratified existing conventions (brownfield, not re-decided): zero build/bundler, plain <script> tags, Node core http server only, flat JSON file persistence per collection dir, CRUD route pattern (id whitelist regex + resolved-path containment + 1MB body limit + setSecurityHeaders), CSP/X-Frame-Options/nosniff headers on every response
- (question) Deferred: real downloadable PDF lib (not needed, print CSS suffices for now); manual drag/drop reorder of recap grid (v2 if requested); match report data structure (backlog per PRD); niveau/catégorie field closed-list vs freetext (PRD open question, no architectural impact either way)
- (event) Reviewer gate: 4 reviews (reconcile, rubric, version-check, adversarial). Real findings: Node floor stale, AD-3 contradicts FR-8 soft-bound, Atelier field ownership undefined, Thématique enum vs existing category taxonomy mismatch, orphan calendar ref timing, derived-value persistence, no-network scope too narrow, FR-5 storage path ambiguous
- (change) Applied fixes: AD-3 reworded (fixed-template grid, not fixed count, resolves FR-8 contradiction), new AD-8 (atelier snapshot-at-add), AD-9 (derived duration never persisted), AD-10 (thématique server-validated enum), AD-11 (no-network scope widened beyond export), AD-5 orphan check now render-time. Node floor corrected ≥18→≥22 (EOL). Consistency Conventions: FR-5 fields optional (SM-C1), storage location flagged. Deferred: category taxonomy migration, FR-5 explanation.doc reconciliation, write durability accepted-risk. Lint clean (0 real findings)
- (event) spine finalized
